package interpret;

import org.apache.commons.cli.Options;

public class Interpret {
	
	Options options = new Options();
	
	/*
	 * options : -sha1, -sha256, -sha384, -sha512, -md5, -pbesha1 -pbesha256, -pbesha384, -pbesha512, -pbemd5
	 * arguments : path to file to code or verify + password (optional)
	 */
	
	public Interpret(String[] args){
		
	}
	
}
